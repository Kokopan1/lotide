// Import Function
const assertEqual = require("./assertEqual");


// Func Check if Arrays are Equal
const eqArrays = function(arr1, arr2) {
  if (arr1.length === arr2.length) {
    for (let i = 0; i < arr1.length; i++) {
      if (arr1[i] !== arr2[i]) {
        return false;
      }
    } return true;
  } return false;
};

// Export Function
module.exports = eqArrays;
